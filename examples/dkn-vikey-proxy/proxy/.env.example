# API key Vikey (wajib)
VIKEY_API_KEY=sk-xxx

# Port proxy (DKN akan mengakses endpoint Ollama di sini)
PORT=14441

# Pemetaan nama model: "Ollama-style" -> "Vikey-style"
# Pastikan nama di kiri cocok dengan DKN_MODELS
MODEL_MAP={"llama3.3:70b-instruct-q4_K_M":"llama-3.3-70b-instruct"}

# Target minimal TPS agar lolos cek performa di DKN (default 12)
# Naikkan jika DKN masih menolak model (mis. 15–20)
PROXY_MIN_TPS=12